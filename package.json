{
  "name": "drupal-rekeningtool",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "bundle": "esbuild index.js --bundle --platform=node --target=node22 --format=cjs --outfile=dist/bundle.cjs",
    "make:exe": "node scripts/make-sea.js",
    "build:exe": "npm run bundle && npm run make:exe"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "@angablue/exe": "^3.2.5",
    "esbuild": "^0.25.11"
  }
}