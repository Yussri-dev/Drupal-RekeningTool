{
  "name": "drupal-rekeningtool",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --target=node22 --minify --outfile=dist/bundle.cjs --format=cjs",
    "make:exe": "npm run build && node scripts/make-sea.js"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.11",
    "nexe": "^5.0.0-beta.4"
  }
}